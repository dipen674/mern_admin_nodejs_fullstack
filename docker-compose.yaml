services:
  mongodb:
    image: mongo:5.0
    container_name: mern-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
      - ./data/load-sample-admin.js:/docker-entrypoint-initdb.d/load-sample-admin.js:ro
    environment:
      - MONGO_INITDB_DATABASE=mern-admin
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 10s
      retries: 5
    networks:
      - mern-network

  backend:
    image: deependrabhatta/githubactions_mern:backend-latest
    container_name: mern-backend
    restart: unless-stopped
    ports:
      - "8888:8888"
    env_file:
      - .variables.env   # non-sensitive configs
    environment:
      - DATABASE=${DATABASE}
      - JWT_SECRET=${JWT_SECRET}
      - SECRET=${SECRET}
      - KEY=${KEY}
    volumes:
      - ./public/uploads:/app/public/uploads
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - mern-network

  frontend:
    image: deependrabhatta/githubactions_mern:frontend-latest
    container_name: mern-frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    env_file:
      - ./frontend/.env   # frontend configs (non-sensitive)
    depends_on:
      - backend
    networks:
      - mern-network

volumes:
  mongodb_data:

networks:
  mern-network:
    driver: bridge
